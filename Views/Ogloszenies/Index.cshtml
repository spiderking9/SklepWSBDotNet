@model IEnumerable<Sklep.Models.CMS.Ogloszenie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tytul)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TrescOgloszenia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tytul)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TrescOgloszenia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdOgloszenia }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdOgloszenia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdOgloszenia })
        </td>
    </tr>
}

</table>
